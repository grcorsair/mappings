# Signers

Public keys used to verify signed mapping packs.

- `grcorsair.pub.pem` is the registry signing key.
- Vendor keys live here as `<vendor>.pub.pem`.

Consumers should verify pack signatures against these keys.
