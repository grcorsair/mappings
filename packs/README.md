# Packs

Each tool version has its own directory:

```
packs/<tool>/<version>/
  mappings/           # mapping JSON files
  fixtures/           # sample inputs + expected outputs
  pack.json           # unsigned pack
  pack.signed.json    # signed pack (generated by maintainers)
```
